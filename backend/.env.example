# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/onyx
DB_HOST=localhost
DB_PORT=5432
DB_NAME=onyx
DB_USER=your_db_user
DB_PASSWORD=your_secure_db_password

# JWT Configuration - GENERATE SECURE SECRETS WITH: openssl rand -base64 64
JWT_SECRET=your-super-secure-jwt-secret-here
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-super-secure-refresh-secret-here
JWT_REFRESH_EXPIRE=30d

# CORS Configuration
CLIENT_URL=http://localhost:5173
# Add custom domains to CORS in server.ts if using custom domain

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_secure_api_secret

# Email Configuration - Mailgun
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-domain.com
MAILGUN_HOST=api.mailgun.net
FROM_EMAIL=noreply@your-domain.com
FROM_NAME=Onyx Assessment Platform

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini